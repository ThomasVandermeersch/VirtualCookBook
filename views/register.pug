
extends layout

block content

    div(
        class='container'
    )
        h3 Login or Register with:

        a(
            href="/auth/facebook"
            classe="btn btn-primary"
        )
            span(
                class="fa fa-facebook-square"
            ) Facebook